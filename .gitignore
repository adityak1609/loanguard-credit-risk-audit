# Raw data — 1.6 GB, download separately (see README)
accepted_2007_to_2018Q4.csv
*.csv.gz
*.zip

# Derived artefacts — regenerate with scripts/prepare_data.py
# Large data files (regenerate with prepare_data.py)
processed/*.parquet

# Serving artefacts are tracked (small, needed by the Streamlit app)
# processed/lgbm_model.pkl
# processed/calibrator.pkl
# processed/feature_spec.json
# processed/serving_config.json

mlruns/
mlflow.db

__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
.venv/

# reports/ is deliberately tracked: those files are the result of the analysis.
